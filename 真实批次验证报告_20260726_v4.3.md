# 链接核验工具真实批次验证报告（4.3 防火墙两步取证）

## 一、结论

4.2 批次中的 769 条“公网不可访问”不能整体解释为网站删除、服务器停机或任何人都无法访问。

这 769 条全部指向 139 个域名、同一公开 IP `119.28.42.49`，并且全部使用 HTTP。本机当前中国移动出口、六个不同代理出口、桌面/手机 User-Agent、HTTP/HTTPS 和境外云线路均未取得正文，主要表现为 502、444、连接关闭或超时。

新的中国普通宽带实测揭示了此前遗漏的访问链路：

1. 中国移动、联通、电信普通宽带首次访问目标原地址；
2. 目标服务器返回 HTTP 403 防火墙挑战、临时 Cookie 和原路径 JavaScript 重定向；
3. 同一探针、同一出口 IP 携带 Cookie 再次请求原地址；
4. 服务器返回 HTTP 200、当前文章标题和正文摘要/正文结构。

因此，此前的大量 502 是“当前出口被拒绝或未完成防火墙会话”，不是足以证明文章删除的证据。

## 二、代表性实测

### 1. 境外普通线路

- 测量 `2ezsrlcskCuv18qu100020pZc`
- 台湾普通运营商线路返回 HTTP 444。
- 页面主体明确写明“您的 IP 归属地不受信任，已被网站防火墙拦截”。

### 2. 中国普通宽带首次挑战

- 测量 `2kgXKscIyGo3DKcJ900020pZd`
- 中国移动台山、武汉和中国电信西安三个普通宽带探针均返回 HTTP 403。
- 响应下发临时 Cookie，并用 JavaScript 跳回同一目标路径。

### 3. 同线路携带 Cookie 后取得正文

真实批次第 12 条，域名 `yuezhanw.zgsuzw.cn`：

- 挑战测量：`2KwudHHoZLmFhA7Ao00020pa1`
- 正文测量：`22gcrL27yZlIRykcM00020pa1`
- 探针：中国联通西安普通宽带
- 二次状态：HTTP 200
- 页面标题：`欧拉系列车型｜女生闭眼入的纯电小车`
- 页面同时返回非空正文摘要。

真实批次第 8 条，域名 `on1ned.echaofs.cn`：

- 挑战测量：`2P19mTWBhc0qp4RQd00020pa2`
- 正文测量：`258cxOz4O7jNwkK0o00020pa2`
- 探针：中国联通南京普通宽带
- 二次状态：HTTP 200
- 页面标题：`欧拉好猫用车成本分析,每月用车花费589元`
- 页面同时返回非空正文摘要。

### 4. 负面对照

使用同一两步流程访问一个人为构造、批次中不存在的文章路径：

- 挑战测量：`27ffWGvYZC552ilr800020pZu`
- 二次测量：`2vGNCQn8CMkEHGaIU00020pZu`
- 二次状态：HTTP 404

这说明新规则不是把所有通过防火墙的响应都判为有效；当前原地址取得 200 和正文证据才判“仍可访问”，404/410 才形成地址失效证据。

## 三、4.3 实现

4.3 在原有本机代理、直连、同站对照之前的结论基础上，新增“中国普通宽带两步取证”：

- 只对共享网媒 502/防火墙候选启用；
- 从中国普通家庭/办公运营商探针发起首访；
- 识别 403/444 防火墙页和 Set-Cookie；
- 固定复用同一探针，携带 Cookie 二次访问原地址；
- 200 响应还必须取得有效文章标题和正文摘要/正文结构；
- 404/410 形成明确失效证据；
- 超时、连接失败、服务额度不足或正文证据不足继续保留未知；
- 结果记录探针国家、城市、运营商和两个公开测量编号；
- 同一域名短时复用防火墙会话，减少一次首访；
- 共享 IP 的熔断改为按域名执行，避免一个域名超时导致另外 138 个域名被整组跳过。

## 四、仍然存在的容量边界

Globalping 匿名调用当前有小时额度，注册 Token 的免费额度也有限。769 条、139 个域名在最坏情况下需要约 908 次探针测试（每个域名一次挑战，加每条链接一次正文请求）。因此：

- 不配置 Token 时，新线路适合自动解决代表样本和一部分批次；
- 配置 `GLOBALPING_API_TOKEN` 后额度更高，但仍可能不足以在同一小时免费跑完全部 769 条；
- 要在单次验收中完整跑完，需要具备足够 credits/自定义额度的 Token，或单位自有的中国大陆普通宽带取证节点；
- 服务返回 429 后，工具会停止继续消耗该线路，不会用额度错误伪造目标结论。

官方说明：

- API 和探针位置选择：<https://github.com/jsdelivr/globalping>
- 普通家庭/办公网络标签：<https://blog.globalping.io/new-globalping-feature-eyeball-and-data-center-network-tags/>

## 五、合同验收口径

4.3 的证据可以分别支持：

- 当前原地址经中国普通宽带、防火墙会话后取得 200 和正文：正文仍在；
- 当前原地址经同样会话后取得 404/410：地址明确失效；
- 只有 502/444/403/超时，没有二次正文：仍是访问受限或待补证，不能判删除；
- 第三方探针服务自身限额或失败：不能归责供应商。

供应商仍应对未完成逐条正文取证的链接补交可访问截图、发布时间、标题、正文片段和可复核访问环境。
